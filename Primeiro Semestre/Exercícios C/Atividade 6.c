#include <stdio.h> // Declarando a biblioteca

int main () { // Início
    
    int i; // Declarando a variavel i, que sera usada no for
    
    for (i = 1; i <= 100; i++) { // Estrutura de laço de repetição
        printf ("%d \n", i); // iImprime o número de 1 a 100
    }
    
return 0; //Fim

}
